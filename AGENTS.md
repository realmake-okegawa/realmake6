# このリポジトリで作業するエージェントへ

**作業を始める前に、必ず `CLAUDE.md` を読んでください。**

このプロジェクトのルールは `CLAUDE.md` に一本化してあります。このファイルは案内だけで、
内容は持ちません。ルールが変わったときは `CLAUDE.md` だけを直してください。

## `CLAUDE.md` に書いてあること

| 節 | 内容 |
|---|---|
| §0 | **既存の正常動作を壊さないためのルール。** 指定された範囲以外のファイルを変更しない。変更前に対象と影響範囲を報告する |
| §2 | 事業者情報（NAP）。**表記ゆれは検索順位を直接下げる**ので、住所・屋号は書き換えない |
| §4 | 施工事例5件の確定データ（塗料名・色番号・艶・費用・工期）。**ここに根拠がない数字を書かない** |
| §5 | 出す数字と出さない数字。**累計施工件数は出さない**など、意図のある取捨選択 |
| §6 | **やってはいけないこと。** 推測を事実のように書かない、数えていない数字を書かない、`aggregateRating` を入れない |
| §7 | ページごとの役割分担。同じ文章を複数ページで使わない |
| §8 | 文章のトーン。断定して書く、煽らない、専門用語に言い換えを添える |
| §9 | サイト構成と、コントラストを下げてはいけない理由 |
| §10 | 未完了タスク（優先順） |

## 公開リポジトリであることに注意

このリポジトリは**公開設定**です。`CLAUDE.md` を含め、コミットしたものは誰でも読めます。

サイトに出さないと決めた情報（保証・許認可・累計施工件数・市場規模の試算・施主から預かった
非公開情報）は `NOTES-private.md` に分けてあり、**このファイルは Git に含まれていません。**
手元にない場合は、その情報を必要とする作業は進めず、大川さんに確認してください。

**新しく機微な情報を書くときは `CLAUDE.md` ではなく `NOTES-private.md` へ。**

## 特に事故が起きやすいところ

- **施工写真の追加** — 電子小黒板に施主の実名が入ります。表札・車のナンバー・洗濯物・室内の写り込みも過去に何度も見落としています。手順と対策は §4 の末尾にまとまっています
- **新規ページの作成** — 計測タグ（`gtag` と `site-analytics.js`）の入れ忘れが実際に2回起きています
- **塗料名** — 日本ペイントは2025年3月24日に「パーフェクトトップ」を「パーフェクトトップSi」へ名称変更しています（§4 冒頭）

## 確認コマンド

変更したら、これを通してから報告してください。

```bash
node scripts/check-site-links.mjs     # 内部リンク
node scripts/check-site-images.mjs    # 画像参照
node scripts/check-sitemap.mjs        # sitemap（未公開URLの404は想定どおり）
```
